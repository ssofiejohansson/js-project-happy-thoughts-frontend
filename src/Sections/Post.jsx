import styled from "styled-components";
import { Form } from "./Components/Form";

const PostContainer = styled.div`
`

export const Post = () => {
  return (
    <PostContainer>

      <Form />
    </PostContainer>
  );
}
